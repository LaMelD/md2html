# md2html.py 는 Python 표준 라이브러리만 사용한다. 외부 패키지 의존성이 없다.
# (argparse, html, os, re, sys, dataclasses, typing — 모두 stdlib)
#
# 이 파일은 "pip install -r requirements.txt" 워크플로를 가정하는 CI/자동화
# 환경과의 호환을 위해 의도적으로 비워둔 placeholder 다.
#
# 요구 Python 버전: 3.7 이상 (dataclasses, `from __future__ import annotations`)
